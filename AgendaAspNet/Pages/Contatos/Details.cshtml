@page
@model AgendaAspNet.Pages.Contatos.DetailsModel

@{
    ViewData["Title"] = "Detalhes do Contato";
}

<h1>@ViewData["Title"]</h1>

<div>
    
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Contato.Nome)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Contato.Nome)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Contato.Telefone)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Contato.Telefone)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-dark" asp-page="./Edit" asp-route-id="@Model.Contato.Id">Editar</a> 
    <a class="btn btn-info" asp-page="./Index">Voltar</a>
</div>